# ---------------------------------
# SERVER ENVIRONMENT VARIABLES
# ---------------------------------

# -- PostgreSQL/Supabase Database --
# Find this in your Supabase project settings: Settings > Database > Connection string > URI
DATABASE_URL="YOUR_SUPABASE_DATABASE_URL"

# -- Google OAuth Credentials --
# Find these in your Google Cloud Console: APIs & Services > Credentials
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"

# -- Backend URL --
# This should match the URL your frontend uses to communicate with the backend.
BACKEND_URL="http://localhost:8080"

# -- Server Port --
PORT=8080
